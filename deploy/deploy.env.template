# deploy.env.template — Configuration for deploy.sh
# Copy to deploy.env (gitignored) and fill in real values.
#
# Usage: ./deploy/deploy.sh deploy/deploy.env

# ── Required ─────────────────────────────────────────────────────────
PROJECT_ID=PROJECT_ID
REGION=us-central1
BUCKET_NAME=BUCKET_NAME
SLACK_CHANNEL=#mail-digest

# ── Optional (with defaults) ────────────────────────────────────────
# REPO_NAME=mail-triage
# JOB_NAME=mail-triage
# SA_NAME=mail-triage-sa
# INPUT_PREFIX=inbox/
# DONE_PREFIX=processed/
# GEMINI_MODEL=gemini-2.5-flash
# SCHEDULER_CRON="*/30 * * * *"
# SCHEDULER_TZ=Asia/Tokyo
# SUMMARY_LANG=ja
# JOB_TIMEOUT=600
# JOB_MEMORY=512Mi
# JOB_CPU=1
